<%-include("../components/head.ejs")%>
  <body>

    <%-include("../components/nav.ejs")%>
    <main>
      <h1><%= title %></h1>

      <% if(peliculas_list.length <= 0) { %>
        <h2>No hay películas registradas</h2>
      <% }  %>

      <table>
          <thead>
              <tr>
                  <th>ID</th>
                  <th>Título</th>
                  <th>Año</th>
                  <th>Duración</th>
                  <th>Categorías</th>
                  <th>Clasificación</th>
                  <th>Director(es)</th>
                  <% if (user && user.rol === 'admin') { %>
                    <th>Administrar</th>
                  <% } %>
              </tr>
          </thead>
          <tbody>
              <% peliculas_list.forEach((peli, i) => { %>
                  <tr>
                      <td><%= peli.id_pelicula %></td>
                      <td><%= peli.titulo %></td>
                      <td><%= peli.anio %></td>
                      <td><%= peli.duracion %></td>
                      <td>
                        <% peli.categorias.forEach((cat, e) => { %>

                          <%if(e != 0){ %> , <% }%>

                          <%=cat.nombre_categoria%>
                          
                        <% }); %>
                      </td>
                      <td><%= peli.clasificacion %></td>

                      <td>
                        <% peli.directores.forEach((dire, j) => { %>

                          <%if(j != 0){ %> , <% }%>

                          <%=dire.nombre %> <%=dire.apellido %>
                          
                        <% }); %>
                      </td>

                      <% if (user && user.rol === 'admin') { %>
                        <td>    
                          <div class="botonera">
                            <a class="botonera__opcion opcion__editar" href="/peliculas/actualizar/<%= peli.id_pelicula %>" class="button">Editar</a>                        
                            <button class="botonera__opcion opcion__eliminar" onclick="eliminarPelicula(this, '<%= peli.id_pelicula %>')"> X </button> 
                          </div>
                        </td>
                      <% } %>

                  </tr>
              <% }); %>
          </tbody>
      </table>
    </main>
        
    <script src="/javascripts/peliculas/eliminar_peliculas.js"></script>

  </body>

<%-include("../components/footer.ejs")%>