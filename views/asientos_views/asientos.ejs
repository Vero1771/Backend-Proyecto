<%-include("../components/head.ejs")%>
  <body>

    <%-include("../components/nav.ejs")%>
    <main>
      <h1><%= title %></h1>

      <% if(asientos_list.length <= 0) { %>
        <h2>No hay asientos registrados</h2>
      <% }  %>

      <table>
          <thead>
              <tr>
                  <th>ID</th>
                  <th>Nombre</th>
                  <th>Administrar</th>
              </tr>
          </thead>
          <tbody>
              <% asientos_list.forEach((a, i) => { %>
                  <tr>
                      <td><%= a.id_asiento %></td>
                      <td><%= a.nombre %></td>
                      <td>    
                        <div class="botonera">
                          <a class="botonera__opcion opcion__editar" href="/asientos/actualizar/<%= a.id_asiento %>" class="button">Editar</a>
                          <button class="botonera__opcion opcion__eliminar" onclick="eliminarAsiento(this, '<%= a.id_asiento %>')"> X </button>
                        </div>
                      </td>
                  </tr>
              <% }); %>
          </tbody>
      </table>

      <script>
        async function eliminarAsiento(boton, id) {
          if (!confirm('¿Estás seguro de eliminar este asiento?')) return;

          try {
            const response = await fetch(`/asientos/eliminar/${id}`, {
              method: 'DELETE', 
              headers: {
                'Content-Type': 'application/json'
              }
            });

            if (response.ok) {
              const fila = boton.closest('tr'); //eliminar la fila de la tabla sin recargar
              fila.remove();
                  
              const tbody = document.querySelector('tbody');
              if (tbody.children.length === 0) {
                location.reload(); 
              }
            } else {
              alert('Error al eliminar el asiento \n Posiblemente tenga relación con otra entidad');
            }
          } catch (error) {
            console.error('Error:', error);
            alert('Hubo un problema con la conexión');
          }
        }
      </script>

    </main>
  </body>

<%-include("../components/footer.ejs")%>