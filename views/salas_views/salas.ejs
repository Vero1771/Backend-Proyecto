<%-include("../components/head.ejs")%>
  <body>

    <%-include("../components/nav.ejs")%>

    <main>
      <h1><%= title %></h1>

      <% if(salas_list.length <= 0) { %>
        <h2>No hay salas registradas</h1>
      <% }  %>

      <table>
          <thead>
              <tr>
                  <th>ID</th>
                  <th>Nombre</th>
                  <th>Capacidad</th>
                  <th>Sucursal</th>
                  <th>Estado</th>
                  <th>Ciudad</th>
                  <th>Dirección</th>
                  <% if (user && user.rol === 'admin') { %>
                    <th>Administrar</th>
                  <% } %>
              </tr>
          </thead>
          <tbody>
              <% salas_list.forEach((s, i) => { %>
                  <tr>
                    <td><%= s.id_sala %></td>
                    <td><%= s.nombre %></td>
                    <td><%= s.capacidad %></td>
                    <td><%= s.nombre_sucursal %></td>
                    <td><%= s.estado %></td>
                    <td><%= s.ciudad %></td>
                    <td><%= s.direccion %></td>
                    
                    <% if (user && user.rol === 'admin') { %>
                      <td>    
                        <div class="botonera">
                          <a class="botonera__opcion opcion__editar" href="/salas/actualizar/<%= s.id_sala %>" class="button">Editar</a>
                          <button class="botonera__opcion opcion__eliminar" onclick="eliminarSala(this, '<%= s.id_sala %>')"> X </button> 
                        </div>
                      </td>
                    <% } %>

                  </tr>
              <% }); %>
          </tbody>
      </table>

    </main>

    <script src="/javascripts/salas/eliminar_salas.js"></script>

  </body>

<%-include("../components/footer.ejs")%>